{
  "vaultPath": "/Users/YOUR-NAME/Documents/Obsidian Vault",
  "rawFolder": "raw",
  "port": 4141,

  "inbox": {
    "enabled": true,
    "file": "clipper-inbox.md"
  },

  "ingest": {
    "_comment": [
      "mode 'inbox' — queue the clip in the inbox file (default, nothing to install)",
      "mode 'auto'  — run an AI agent straight after saving",
      "mode 'off'   — do neither",
      "",
      "command 'auto' means: use whichever agent CLI is actually installed.",
      "Claude, Gemini, Codex, OpenCode, Cursor and Aider are all recognised, so",
      "switching AI is a matter of installing a different one — no config edit,",
      "no code change. Name a command explicitly (or give a path to your own",
      "script) to override the search.",
      "",
      "The agent runs with the vault as its working directory, and {{path}} is",
      "replaced with the new file's path inside the vault."
    ],
    "mode": "inbox",
    "command": "auto",
    "promptTemplate": "ingest {{path}}",
    "timeoutMinutes": 20
  }
}
